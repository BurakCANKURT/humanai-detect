data_root: data
external_dir: data/external
raw_dir: data/raw
interim_dir: data/interim
processed_dir: data/processed

outputs_root: outputs
models_dir: outputs/models
figures_dir: outputs/figures
reports_dir: outputs/reports

hf_cache_dir: null  # null -> transformers varsayilani (HF_HOME env ile override edilebilir)

lexicon_dir: data/processed/lexicons  # mine_contrastive_lexicons.py ciktisi (ai_cliche/human_informality n-gram JSON'lari)
